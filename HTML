<!DOCTYPE html>

<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fiche d'Heures Chauffeur</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

```
    body {
        font-family: 'Arial', sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        padding: 20px;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        overflow: hidden;
    }

    .header {
        background: linear-gradient(135deg, #2c3e50, #3498db);
        color: white;
        padding: 30px;
        text-align: center;
        position: relative;
        overflow: hidden;
    }

    .header::before {
        content: '';
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: linear-gradient(45deg, transparent, rgba(255,255,255,0.1), transparent);
        animation: shine 3s infinite;
    }

    @keyframes shine {
        0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
        100% { transform: translateX(100%) translateY(100%) rotate(45deg); }
    }

    .header h1 {
        font-size: 2.5em;
        font-weight: bold;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        position: relative;
        z-index: 1;
    }

    .form-section {
        padding: 30px;
        border-bottom: 1px solid #eee;
    }

    .employee-info {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
        margin-bottom: 30px;
    }

    .form-group {
        display: flex;
        flex-direction: column;
    }

    .form-group label {
        font-weight: bold;
        margin-bottom: 8px;
        color: #2c3e50;
        font-size: 1.1em;
    }

    .form-group input, .form-group select {
        padding: 12px 15px;
        border: 2px solid #e0e0e0;
        border-radius: 10px;
        font-size: 16px;
        transition: all 0.3s ease;
        background: rgba(255, 255, 255, 0.9);
    }

    .form-group input:focus, .form-group select:focus {
        outline: none;
        border-color: #3498db;
        box-shadow: 0 0 15px rgba(52, 152, 219, 0.2);
        transform: translateY(-2px);
    }

    .timesheet-table {
        width: 100%;
        overflow-x: auto;
        margin: 20px 0;
    }

    table {
        width: 100%;
        border-collapse: collapse;
        background: white;
        border-radius: 15px;
        overflow: hidden;
        box-shadow: 0 10px 25px rgba(0,0,0,0.1);
    }

    th {
        background: linear-gradient(135deg, #34495e, #2c3e50);
        color: white;
        padding: 15px 10px;
        text-align: center;
        font-weight: bold;
        position: relative;
    }

    td {
        padding: 12px 8px;
        text-align: center;
        border-bottom: 1px solid #f0f0f0;
        transition: background-color 0.2s ease;
    }

    tr:hover td {
        background-color: rgba(52, 152, 219, 0.05);
    }

    .day-cell {
        background-color: #f8f9fa;
        font-weight: bold;
        color: #2c3e50;
    }

    .time-input {
        width: 80px;
        padding: 8px;
        border: 1px solid #ddd;
        border-radius: 5px;
        text-align: center;
        font-size: 14px;
    }

    .time-input:focus {
        border-color: #3498db;
        box-shadow: 0 0 5px rgba(52, 152, 219, 0.3);
    }

    .checkbox-cell input[type="checkbox"] {
        transform: scale(1.5);
        accent-color: #3498db;
    }

    .notes-input {
        width: 100px;
        padding: 8px;
        border: 1px solid #ddd;
        border-radius: 5px;
        font-size: 14px;
    }

    .total-row {
        background: linear-gradient(135deg, #e8f4fd, #d1ecf1);
        font-weight: bold;
        color: #2c3e50;
    }

    .total-row td {
        padding: 15px;
        font-size: 1.1em;
    }

    .actions {
        padding: 30px;
        display: flex;
        gap: 15px;
        flex-wrap: wrap;
        justify-content: center;
    }

    .btn {
        padding: 15px 30px;
        border: none;
        border-radius: 50px;
        font-size: 16px;
        font-weight: bold;
        cursor: pointer;
        transition: all 0.3s ease;
        text-transform: uppercase;
        letter-spacing: 1px;
        box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        position: relative;
        overflow: hidden;
    }

    .btn::before {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 0;
        height: 0;
        background: rgba(255,255,255,0.2);
        border-radius: 50%;
        transition: all 0.3s ease;
        transform: translate(-50%, -50%);
    }

    .btn:hover::before {
        width: 300px;
        height: 300px;
    }

    .btn-primary {
        background: linear-gradient(135deg, #3498db, #2980b9);
        color: white;
    }

    .btn-primary:hover {
        transform: translateY(-3px);
        box-shadow: 0 10px 25px rgba(52, 152, 219, 0.3);
    }

    .btn-success {
        background: linear-gradient(135deg, #27ae60, #229954);
        color: white;
    }

    .btn-success:hover {
        transform: translateY(-3px);
        box-shadow: 0 10px 25px rgba(39, 174, 96, 0.3);
    }

    .btn-warning {
        background: linear-gradient(135deg, #f39c12, #e67e22);
        color: white;
    }

    .btn-warning:hover {
        transform: translateY(-3px);
        box-shadow: 0 10px 25px rgba(243, 156, 18, 0.3);
    }

    .observations {
        margin: 20px 0;
    }

    .observations textarea {
        width: 100%;
        min-height: 100px;
        padding: 15px;
        border: 2px solid #e0e0e0;
        border-radius: 10px;
        font-size: 16px;
        font-family: Arial, sans-serif;
        resize: vertical;
        transition: all 0.3s ease;
    }

    .observations textarea:focus {
        outline: none;
        border-color: #3498db;
        box-shadow: 0 0 15px rgba(52, 152, 219, 0.2);
    }

    .success-message {
        background: linear-gradient(135deg, #d4edda, #c3e6cb);
        color: #155724;
        padding: 15px;
        border-radius: 10px;
        margin: 20px 0;
        text-align: center;
        font-weight: bold;
        display: none;
        animation: slideIn 0.5s ease;
    }

    @keyframes slideIn {
        from { opacity: 0; transform: translateY(-20px); }
        to { opacity: 1; transform: translateY(0); }
    }

    @media (max-width: 768px) {
        .header h1 {
            font-size: 2em;
        }
        
        .employee-info {
            grid-template-columns: 1fr;
        }
        
        .timesheet-table {
            font-size: 14px;
        }
        
        .time-input {
            width: 70px;
            font-size: 12px;
        }
        
        .actions {
            flex-direction: column;
            align-items: center;
        }
        
        .btn {
            width: 100%;
            max-width: 300px;
        }
    }
</style>
```

</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🚛 FICHE D'HEURES CHAUFFEUR</h1>
        </div>

```
    <div class="form-section">
        <div class="employee-info">
            <div class="form-group">
                <label for="lastName">Nom:</label>
                <input type="text" id="lastName" placeholder="Nom de famille">
            </div>
            <div class="form-group">
                <label for="firstName">Prénom:</label>
                <input type="text" id="firstName" placeholder="Prénom">
            </div>
            <div class="form-group">
                <label for="month">Mois:</label>
                <select id="month">
                    <option value="">Sélectionner un mois</option>
                    <option value="Janvier">Janvier</option>
                    <option value="Février">Février</option>
                    <option value="Mars">Mars</option>
                    <option value="Avril">Avril</option>
                    <option value="Mai">Mai</option>
                    <option value="Juin">Juin</option>
                    <option value="Juillet">Juillet</option>
                    <option value="Août">Août</option>
                    <option value="Septembre">Septembre</option>
                    <option value="Octobre">Octobre</option>
                    <option value="Novembre">Novembre</option>
                    <option value="Décembre">Décembre</option>
                </select>
            </div>
            <div class="form-group">
                <label for="year">Année:</label>
                <input type="number" id="year" value="2025" min="2020" max="2030">
            </div>
        </div>
    </div>

    <div class="form-section">
        <div class="timesheet-table">
            <table id="timesheetTable">
                <thead>
                    <tr>
                        <th>Jours</th>
                        <th>Heure de départ</th>
                        <th>Heure d'arrivée</th>
                        <th>Temps de pause</th>
                        <th>Temps de service</th>
                        <th>Repas</th>
                        <th>Découchés</th>
                        <th>Divers</th>
                    </tr>
                </thead>
                <tbody id="timesheetBody">
                    <!-- Les lignes seront générées dynamiquement -->
                </tbody>
                <tfoot>
                    <tr class="total-row">
                        <td><strong>TOTAL</strong></td>
                        <td></td>
                        <td></td>
                        <td id="totalBreaks"><strong>0h00</strong></td>
                        <td id="totalHours"><strong>0h00</strong></td>
                        <td id="totalMeals"><strong>0</strong></td>
                        <td id="totalOvernights"><strong>0</strong></td>
                        <td></td>
                    </tr>
                </tfoot>
            </table>
        </div>

        <div class="observations">
            <label for="observations"><strong>Observations:</strong></label>
            <textarea id="observations" placeholder="Ajoutez vos observations ici..."></textarea>
        </div>
    </div>

    <div class="success-message" id="successMessage">
        ✅ Données sauvegardées avec succès !
    </div>

    <div class="actions">
        <button class="btn btn-primary" onclick="calculateTotal()">📊 Calculer Total</button>
        <button class="btn btn-success" onclick="exportToEmail()">📧 Envoyer par Email</button>
        <button class="btn btn-warning" onclick="printTimesheet()">🖨️ Imprimer</button>
        <button class="btn btn-primary" onclick="saveData()">💾 Sauvegarder</button>
    </div>
</div>

<script>
    // Générer les jours du mois
    function generateDays() {
        const tbody = document.getElementById('timesheetBody');
        tbody.innerHTML = '';
        
        for (let day = 1; day <= 31; day++) {
            const row = document.createElement('tr');
            row.innerHTML = `
                <td class="day-cell">${day}</td>
                <td><input type="time" class="time-input" id="start_${day}" onchange="calculateDayHours(${day})"></td>
                <td><input type="time" class="time-input" id="end_${day}" onchange="calculateDayHours(${day})"></td>
                <td><input type="text" class="time-input" id="break_${day}" placeholder="0h00" onchange="calculateDayHours(${day})"></td>
                <td id="service_${day}">0h00</td>
                <td class="checkbox-cell"><input type="checkbox" id="meal_${day}" onchange="calculateTotal()"></td>
                <td class="checkbox-cell"><input type="checkbox" id="overnight_${day}" onchange="calculateTotal()"></td>
                <td><input type="text" class="notes-input" id="misc_${day}" placeholder="Divers"></td>
            `;
            tbody.appendChild(row);
        }
    }

    // Calculer les heures pour une journée
    function calculateDayHours(day) {
        const startTime = document.getElementById(`start_${day}`).value;
        const endTime = document.getElementById(`end_${day}`).value;
        const breakTime = document.getElementById(`break_${day}`).value;
        
        if (startTime && endTime) {
            const start = new Date(`2000-01-01T${startTime}:00`);
            const end = new Date(`2000-01-01T${endTime}:00`);
            
            let diff = end - start;
            if (diff < 0) {
                diff += 24 * 60 * 60 * 1000; // Ajouter 24h si passage minuit
            }
            
            let totalMinutes = Math.floor(diff / (1000 * 60));
            
            // Déduire le temps de pause
            if (breakTime) {
                const breakMinutes = parseBreakTime(breakTime);
                totalMinutes -= breakMinutes;
            }
            
            // S'assurer que le temps de service n'est pas négatif
            totalMinutes = Math.max(0, totalMinutes);
            
            const hours = Math.floor(totalMinutes / 60);
            const minutes = totalMinutes % 60;
            
            document.getElementById(`service_${day}`).textContent = `${hours}h${minutes.toString().padStart(2, '0')}`;
        } else {
            document.getElementById(`service_${day}`).textContent = '0h00';
        }
    }
    
    // Parser le temps de pause (accepte format "2h30" ou "2:30" ou "150" minutes)
    function parseBreakTime(timeStr) {
        if (!timeStr) return 0;
        
        timeStr = timeStr.trim().toLowerCase();
        
        // Format "2h30"
        if (timeStr.includes('h')) {
            const parts = timeStr.split('h');
            const hours = parseInt(parts[0]) || 0;
            const minutes = parseInt(parts[1]) || 0;
            return hours * 60 + minutes;
        }
        
        // Format "2:30"
        if (timeStr.includes(':')) {
            const parts = timeStr.split(':');
            const hours = parseInt(parts[0]) || 0;
            const minutes = parseInt(parts[1]) || 0;
            return hours * 60 + minutes;
        }
        
        // Format minutes seulement
        return parseInt(timeStr) || 0;
    }

    // Calculer le total des heures
    function calculateTotal() {
        let totalServiceMinutes = 0;
        let totalBreakMinutes = 0;
        let totalMeals = 0;
        let totalOvernights = 0;
        
        for (let day = 1; day <= 31; day++) {
            // Total temps de service
            const serviceText = document.getElementById(`service_${day}`).textContent;
            if (serviceText !== '0h00') {
                const [hours, minutes] = serviceText.replace('h', ':').split(':').map(Number);
                totalServiceMinutes += hours * 60 + minutes;
            }
            
            // Total temps de pause
            const breakTime = document.getElementById(`break_${day}`).value;
            if (breakTime) {
                totalBreakMinutes += parseBreakTime(breakTime);
            }
            
            // Total repas
            if (document.getElementById(`meal_${day}`).checked) {
                totalMeals++;
            }
            
            // Total découchés
            if (document.getElementById(`overnight_${day}`).checked) {
                totalOvernights++;
            }
        }
        
        // Afficher temps de service total
        const serviceHours = Math.floor(totalServiceMinutes / 60);
        const serviceMinutes = totalServiceMinutes % 60;
        document.getElementById('totalHours').innerHTML = `<strong>${serviceHours}h${serviceMinutes.toString().padStart(2, '0')}</strong>`;
        
        // Afficher temps de pause total
        const breakHours = Math.floor(totalBreakMinutes / 60);
        const breakMinutes = totalBreakMinutes % 60;
        document.getElementById('totalBreaks').innerHTML = `<strong>${breakHours}h${breakMinutes.toString().padStart(2, '0')}</strong>`;
        
        // Afficher totaux repas et découchés
        document.getElementById('totalMeals').innerHTML = `<strong>${totalMeals}</strong>`;
        document.getElementById('totalOvernights').innerHTML = `<strong>${totalOvernights}</strong>`;
    }

    // Sauvegarder les données
    function saveData() {
        const data = {
            lastName: document.getElementById('lastName').value,
            firstName: document.getElementById('firstName').value,
            month: document.getElementById('month').value,
            year: document.getElementById('year').value,
            observations: document.getElementById('observations').value,
            days: {}
        };
        
        for (let day = 1; day <= 31; day++) {
            data.days[day] = {
                start: document.getElementById(`start_${day}`).value,
                end: document.getElementById(`end_${day}`).value,
                break: document.getElementById(`break_${day}`).value,
                service: document.getElementById(`service_${day}`).textContent,
                meal: document.getElementById(`meal_${day}`).checked,
                overnight: document.getElementById(`overnight_${day}`).checked,
                misc: document.getElementById(`misc_${day}`).value
            };
        }
        
        // Simuler la sauvegarde
        const successMessage = document.getElementById('successMessage');
        successMessage.style.display = 'block';
        setTimeout(() => {
            successMessage.style.display = 'none';
        }, 3000);
        
        console.log('Données sauvegardées:', data);
    }

    // Exporter vers email
    function exportToEmail() {
        calculateTotal();
        
        const lastName = document.getElementById('lastName').value;
        const firstName = document.getElementById('firstName').value;
        const month = document.getElementById('month').value;
        const year = document.getElementById('year').value;
        const totalHours = document.getElementById('totalHours').textContent;
        const totalBreaks = document.getElementById('totalBreaks').textContent;
        const totalMeals = document.getElementById('totalMeals').textContent;
        const totalOvernights = document.getElementById('totalOvernights').textContent;
        
        let emailBody = `Fiche d'heures - ${firstName} ${lastName}\n`;
        emailBody += `Période: ${month} ${year}\n\n`;
        emailBody += `TOTAUX:\n`;
        emailBody += `- Temps de service: ${totalHours}\n`;
        emailBody += `- Temps de pause: ${totalBreaks}\n`;
        emailBody += `- Repas: ${totalMeals}\n`;
        emailBody += `- Découchés: ${totalOvernights}\n\n`;
        emailBody += `Détail des journées:\n`;
        
        for (let day = 1; day <= 31; day++) {
            const start = document.getElementById(`start_${day}`).value;
            const end = document.getElementById(`end_${day}`).value;
            const breakTime = document.getElementById(`break_${day}`).value;
            const service = document.getElementById(`service_${day}`).textContent;
            
            if (start || end) {
                emailBody += `Jour ${day}: ${start} - ${end}`;
                if (breakTime) emailBody += ` (pause: ${breakTime})`;
                emailBody += ` = ${service}\n`;
            }
        }
        
        const observations = document.getElementById('observations').value;
        if (observations) {
            emailBody += `\nObservations: ${observations}`;
        }
        
        const subject = `Fiche d'heures ${firstName} ${lastName} - ${month} ${year}`;
        const mailtoLink = `mailto:?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(emailBody)}`;
        
        window.location.href = mailtoLink;
    }

    // Imprimer la fiche
    function printTimesheet() {
        window.print();
    }

    // Initialiser la page
    document.addEventListener('DOMContentLoaded', function() {
        generateDays();
        
        // Définir le mois et l'année actuels
        const now = new Date();
        const currentMonth = now.toLocaleString('fr-FR', { month: 'long' });
        const currentYear = now.getFullYear();
        
        document.getElementById('month').value = currentMonth.charAt(0).toUpperCase() + currentMonth.slice(1);
        document.getElementById('year').value = currentYear;
    });

    // Style d'impression
    const printStyles = `
        @media print {
            body { background: white !important; }
            .container { box-shadow: none !important; }
            .header { background: #2c3e50 !important; }
            .actions { display: none !important; }
            .success-message { display: none !important; }
            table { page-break-inside: avoid; }
        }
    `;
    
    const styleSheet = document.createElement('style');
    styleSheet.textContent = printStyles;
    document.head.appendChild(styleSheet);
</script>
```

</body>
</html>
